name: Frontend CI

on:
  pull_request:
  push:
    branches: [main]
    paths:
      - "artifacts/**"
      - "lib/**"
      - "scripts/**"
      - "docs/**"
      - "pilot/**"
      - "intelligence/**"
      - "EQUIVISTA_CANONICAL_ROADMAP.md"
      - ".npmrc"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/frontend-ci.yml"

permissions:
  contents: read

concurrency:
  group: frontend-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend-verify:
    name: frontend-verify
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      CI: "true"
      PORT: "4173"
      BASE_PATH: "/"
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@a0853c24544627f65ddf259abe73b1d18a591444 # v5
        with:
          node-version: 22
      - name: Activate pinned pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.16.0 --activate
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Typecheck
        run: pnpm typecheck
      - name: Test production web server
        run: pnpm --filter @workspace/equus-voyages test:server
      - name: Verify controlled-pilot foundation
        run: pnpm verify:pilot && pnpm test:pilot && pnpm verify:batch6 && pnpm test:batch6 && pnpm verify:batch7 && pnpm test:batch7 && pnpm verify:batch8 && pnpm test:batch8 && pnpm verify:batch9 && pnpm test:batch9 && pnpm verify:batch10 && pnpm test:batch10 && pnpm verify:batch11 && pnpm test:batch11 && pnpm verify:batch12 && pnpm test:batch12 && pnpm verify:batch13 && pnpm test:batch13 && pnpm verify:batch14 && pnpm test:batch14 && pnpm verify:batch15 && pnpm test:batch15 && pnpm verify:batch16 && pnpm test:batch16 && pnpm verify:batch17 && pnpm test:batch17 && pnpm verify:batch18 && pnpm test:batch18 && pnpm verify:batch19 && pnpm test:batch19 && pnpm verify:batch20 && pnpm test:batch20
      - name: Build
        run: pnpm build
