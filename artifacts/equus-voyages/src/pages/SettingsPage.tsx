import { Settings } from "lucide-react";
import FeaturePlaceholder from "@/components/FeaturePlaceholder";

export default function SettingsPage() {
  return <FeaturePlaceholder icon={Settings} titleKey="nav.settings" />;
}
