import { Heart } from "lucide-react";
import FeaturePlaceholder from "@/components/FeaturePlaceholder";

export default function HorsesPage() {
  return <FeaturePlaceholder icon={Heart} titleKey="nav.horses" />;
}
