import{readFile}from"node:fs/promises";import{evaluateContinuityAttestation,validateBatch19Config}from"./batch19-continuity-attestation.mjs";const p=process.argv[2];if(!p)process.exit(2);const c=JSON.parse(await readFile(p,"utf8")),e=validateBatch19Config(c),s=c.fixtures.find(x=>x.fixture_ref==="attestation-synthetic-ready")?.input;function m(r){if(!r?.source_fixture_ref)return structuredClone(r);const i=structuredClone(s);if(r.mutation==="recovery")i.batch18_recovery.recovery_hash="0".repeat(64);if(r.mutation==="objective")i.attestation.rto_observed_minutes=61;if(r.mutation==="finding")i.attestation.open_critical_findings=1;if(r.mutation==="signoff")i.attestation.signoffs=[];if(r.mutation==="followup")i.attestation.next_drill_days=365;if(r.mutation==="mutation")i.attestation.writeback=true;return i}for(const f of c.fixtures??[]){const r=evaluateContinuityAttestation(m(f.input),c);if(r.decision!==f.expected_decision||(f.expected_code&&!r.rejection_codes.includes(f.expected_code)))e.push(`${f.fixture_ref} failed`)}if(e.length){e.forEach(console.error);process.exit(1)}console.log(`Batch 19 offline continuity-attestation package is valid: ${p}`);
