import{readFile}from"node:fs/promises";import{evaluateProgramCloseout,validateBatch20Config}from"./batch20-program-closeout.mjs";const p=process.argv[2],c=JSON.parse(await readFile(p,"utf8")),e=validateBatch20Config(c),s=c.fixtures[0].input;for(const f of c.fixtures){let i=structuredClone(f.input.source_fixture_ref?s:f.input);if(f.input.mutation==="attestation")i.batch19_attestation.attestation_hash="0".repeat(64);if(f.input.mutation==="evidence")i.closeout.evidence_hashes=[];if(f.input.mutation==="finding")i.closeout.unresolved_findings=1;if(f.input.mutation==="approval")i.closeout.approvals=[];if(f.input.mutation==="retention")i.closeout.retention_days=1;if(f.input.mutation==="release")i.closeout.release_authorized=true;const z=evaluateProgramCloseout(i,c);if(z.decision!==f.expected_decision||(f.expected_code&&!z.rejection_codes.includes(f.expected_code)))e.push(f.fixture_ref)}if(e.length){e.forEach(console.error);process.exit(1)}console.log(`Batch 20 offline program-closeout package is valid: ${p}`)
